@model IEnumerable<MVC.Models.Achivment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Game.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AchivmentTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AchivmentDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Game.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AchivmentTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AchivmentDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AchivmentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AchivmentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AchivmentID })
        </td>
    </tr>
}

</table>
